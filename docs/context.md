# Контекст и глобальные состояния

В этом разделе описаны глобальные состояния приложения, реализованные через **React Context API**.
Контексты позволяют управлять общими данными и сохранять их между компонентами без необходимости передавать props вручную.

---

## 📁 Структура контекстов

```
src/context/
└── FiltersContext.tsx
```

---

## FiltersContext.tsx

**Назначение:**
Обеспечивает глобальное состояние фильтров, применяемых к спискам данных (врачи, приёмы и т.д.).
Используется для синхронизации выбранных фильтров между разными компонентами, такими как `TopFilters`, `AppointmentsTable`, `DoctorsTable`.

**Основные функции и значения:**

* `filters` — текущее состояние выбранных фильтров (объект).
* `setFilters()` — функция обновления состояния фильтров.
* `resetFilters()` — сброс фильтров к значениям по умолчанию.

**Использование:**

```tsx
import { useFilters } from "@/context/FiltersContext";

const { filters, setFilters } = useFilters();

setFilters({ doctor: "Dr. Smith", status: "active" });
```

**Применение в компонентах:**

* `TopFiltersClient.tsx` — интерфейс выбора фильтров пользователем.
* `AppointmentsTable.tsx` и `DoctorsTable.tsx` — используют активные фильтры для отображения отфильтрованных данных.

---

## Паттерн использования

Контекст и хук `useFilters()` экспортируются для подключения в любом компоненте приложения.
Таким образом, при изменении фильтров обновляются все компоненты, зависящие от этих данных.

---

## Потенциал расширения

В будущем можно добавить:

* Контекст для авторизации (`AuthContext`);
* Контекст темы оформления (`ThemeContext`);
* Контекст данных пользователя (`UserContext`).

Эта архитектура масштабируема.
