# Утилиты и функции прикладного уровня

В этой папке находятся небольшие вспомогательные функции, которые используются для обработки данных в разных частях приложения.
Они не зависят от UI и обычно представляют собой чистые функции.

---

## 📁 Структура

```
src/utils/
└── applyFilters.ts
```

---

## applyFilters.ts

**Назначение:**
Реализует общую логику фильтрации данных (например, списка приёмов или врачей) по заданным критериям.

**Основные функции:**

* `applyFilters(data, filters)` — принимает массив данных и объект фильтров, возвращает только те элементы, которые удовлетворяют условиям.

**Пример использования:**

```ts
import { applyFilters } from "@/utils/applyFilters";

const filtered = applyFilters(appointments, { doctor: "Dr. Smith", status: "active" });
```

**Применяется в:**

* `AppointmentsTable.tsx`
* `DoctorsTable.tsx`
* `TopFiltersClient.tsx`

---

## Принципы

* Логика независима от интерфейса и может быть протестирована отдельно.
* Следует принципу *Single Responsibility* — каждая утилита выполняет только одну задачу.
* Функции можно легко переиспользовать в других проектах или сервисах.

---

## Возможности для расширения

В будущем в `utils/` можно добавить:

* `formatters.ts` — функции форматирования чисел, дат и строк;
* `validators.ts` — проверки корректности вводимых данных;
* `math.ts` — вычислительные утилиты для аналитических модулей.

Папка `utils` служит универсальным хранилищем общей логики, обеспечивая чистоту и переиспользуемость кода.
