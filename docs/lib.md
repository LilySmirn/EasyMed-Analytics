# Вспомогательные библиотеки и функции

В этой части проекта собраны модули, которые обеспечивают повторно используемую логику, математические и графические вычисления, а также общие вспомогательные функции.

---

## 📁 Структура библиотеки

```
src/lib/
├── chartUtils.ts
└── utils.ts
```

---

## chartUtils.ts

**Назначение:**
Содержит функции для подготовки и форматирования данных для визуализации графиков и диаграмм.
Используется в компонентах, построенных на библиотеке **Tremor**, для работы с графиками и отображения аналитических данных.

**Основные функции:**

* `transformChartData()` — преобразует данные в формат, понятный компонентам Tremor.
* `formatValue()` — форматирует числовые значения (например, проценты, валюту, округление).
* `getChartColors()` — возвращает палитру цветов для разных типов диаграмм.

**Пример использования:**

```ts
import { transformChartData } from "@/lib/chartUtils";

const chartData = transformChartData(appointments);
```

**Используется в:**

* `AppointmentsTable.tsx`
* `DoctorsTable.tsx`
* Виджетах аналитики и отчётности

---

## utils.ts

**Назначение:**
Общие утилиты, не привязанные к визуальной части или конкретному модулю.
Содержит вспомогательные функции для работы с массивами, объектами и форматированием данных.

**Пример использования:**

```ts
import { formatDate } from "@/lib/utils";

const formatted = formatDate("2025-11-06");
```

---

## Применение в архитектуре

Модули `lib` — это **ядро утилитарной логики**, не зависящее от UI.
Они используются компонентами, API и контекстами, обеспечивая единообразие и чистоту кода.

---

## Потенциал расширения

Можно добавить:

* `apiClient.ts` — единый HTTP-клиент для работы с API.
* `dataTransform.ts` — для сложных преобразований аналитических данных.
* `validation.ts` — функции проверки данных и форм.

Библиотека `lib` служит фундаментом для масштабирования проекта без дублирования логики.
